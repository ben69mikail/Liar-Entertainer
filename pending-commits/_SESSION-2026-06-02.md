# Session-Zusammenfassung 2026-06-02

## Ausgangslage
Michael hatte Ordner von `C:\Users\ben_m\.claude` nach `C:\Users\ben_m\Dev-Claude`
verschoben. Dadurch brachen Pfade in Configs, Scheduled-Tasks und Doku.

## Was in dieser Session erledigt wurde

### Struktur & Pfade
- **Zwei-Ordner-Prinzip etabliert:** `Cerveau I` = Wissen/Steuerung (Vault), `Dev-Claude` = Werkstatt (Code/Workspaces).
- `Dev-Claude\STRUKTUR-LANDKARTE.md` als zentrale Landkarte erstellt.
- Vault `CLAUDE.md` + `STRUKTUR.md` auf neue Pfade aktualisiert.
- 18 veraltete Projekt-`CLAUDE.md` im Vault bereinigt (alte .claude/OneDrive-Pfade → Dev-Claude).

### Scheduled-Tasks (3 aktiv, alle repariert)
Registry-Pfad bleibt fix `C:\Users\ben_m\Claude\Scheduled\` (NICHT verschieben).
- `liar-daily-seo-autopilot-v2` (06:00) — Prompt self-contained, Repo ben69mikail/Liar-Entertainer, Workspace Dev-Claude.
- `gbp-quota-approval-check` (09:00) — Pfade aktualisiert.
- `nas-backup-nightly` (02:00) — Skript intakt; backup-nas.ps1 um `Dev-Claude` als Backup-Quelle erweitert.
- NEU: `github-pat-waechter` (Mo 08:10) — prüft wöchentlich GitHub-Schreibzugriff.

### Obsidian → GitHub
- `.gitignore` im Vault angelegt (schützt .secrets, .env, Tokens — per git check-ignore verifiziert).
- `Obsidian-Git-Einrichtung.md` als Schritt-Anleitung (privates Repo cerveau-vault + Auto-Versionierung).

### LIAR-Doku
- Vault-Kopie `Projekte\Homepage-LIAR\` aufgeräumt: 18 Altdateien → `_archiv/`, CLAUDE.md = schlanke Verweisnotiz.

## OFFEN — Pending-Commits deployen (BLOCKER: GitHub-Auth)
GitHub-MCP: **Lesen funktioniert, Schreiben scheitert** mit `Authentication Failed`.
Ursache: Diese Session hält den alten (abgelaufenen) Token; neuer Token wird erst in
einer FRISCHEN Konversation geladen. Token wurde erneuert + als Umgebungsvariable
GITHUB_PERSONAL_ACCESS_TOKEN gesetzt, App neu gestartet — diese Session lädt ihn aber nicht.

→ **In neuem Chat pushen.** Prompt liegt in `_PROMPT-NEUER-CHAT.md`.
→ 3 offene Commits: City-410 .htaccess, robots.txt attachment-disallow, sitemap.xml.ts redirect-filter.
→ Details: `_DEPLOY-JETZT_2026-06-02.md`.

⚠️ Sicherheit: Ein Token wurde versehentlich im Chat eingegeben → wurde widerrufen + ersetzt.
Falls neuer Token auch scheitert: CLASSIC PAT mit `repo`-Scope nötig (MCP nutzt api.githubcopilot.com).
